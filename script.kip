// Test file for FileTest.java

def memes(x) {
    if (x > 5) {
        return "wew"
        println "should not do this at all"
    } else if (x < 0) {
        return "meme"
        println "should not do this at all"
    } else {
        if (x == 2) {
            return "ugh"
            println "should not do this at all"
        }
        return "whatever"
        println "should not do this at all"
    }
}

[-5..10] |> Array.map(memes)